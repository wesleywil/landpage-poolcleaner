---
interface Props {
  question: string;
  answer: string;
}

const { question, answer } = Astro.props;
---

<div class="xl:w-1/2 p-2 flex flex-col gap-2 items-center text-center">
  <h1 class="text-2xl font-semibold">{question}</h1>
  <p>{answer}</p>
</div>
