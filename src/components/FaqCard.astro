---
interface Props {
  question: string;
  answer: string;
}

const { question, answer } = Astro.props;
---

<div class="xl:w-1/2 p-2 flex flex-col gap-2 items-center text-center">
  <h1 class="text-[#00b2e1] text-2xl font-semibold">{question}</h1>
  <p class="text-white">{answer}</p>
</div>
